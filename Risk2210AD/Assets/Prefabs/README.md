# Prefabs

Territory cells, MOD counters, commander badges, transport orbs, shockwave rings and dice are
built procedurally by the View layer (`TerritoryView.Create`, `AnimationDirector`, `CombatOverlay`).
Drop replacement prefabs here and swap the factory calls if you want hand-authored art.
